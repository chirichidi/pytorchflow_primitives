# 필요한 패키지 import

# 저장한 hyperparameter 불러오기

# 1. Test data 를 위한 경우 -> 구현은 안함
    # 기존에 만들었던 dataloader를 그대로 가져오면 됨
    # test.py 와 같은 파일로 따로 보관
# 2. 실제 데이터를 받는 경우
# 학습했던 data 전처리 과정을 그대로 가져와야함
# dataloader 의 역할에 상응하는 다른 모듈을 만들어야함

# 이미 만들어진 ai 모델 설계도를 가져오고

# ai 모델 객체 생성 (과정에서 hyperparameter 사용)
# 학습된 weight 를 생성한 ai 모델 객체에 넣어주기

# 준비한 데이터를 AI 모델에 넣어주기
# 결과로 나온 데이터를 해석 (시각화)
